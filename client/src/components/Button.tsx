import styled from "styled-components"
const Button = styled.button`
	padding: 8px 16px;
	background: #eff3f4;
	border: none;
	border-radius: 64px;
	color: black;
	font-family: inherit;
	font-weight: bold;
	cursor: pointer;
`
export default Button